#In a stack, the last element added (or “pushed”) is the first one to be removed (or “popped”).
# This is known as the Last In, First Out principle (LIFO).
# in a stack the add process called (pushed); the remove process called (popped).
#a group of boxes stacked on top of each other.

#In a queue, the first element added (or “enqueued”) is the first one to be removed (or “dequeued”).
# This follows the First In, First Out principle (FIFO).
# in a queue the add process called (enqueue); the remove process called (dequeue).
#the queue on gas station.