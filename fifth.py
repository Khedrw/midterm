#In a stack, the last element we added  is the first one we removed.
#last in first out (LIFO).
# in a stack the add process called (pushed); the remove process called (popped).


#In a queue, the first element we added is the first one we removed.
#first in first out (FIFO).
# in a queue the add process called (enqueue); the remove process called (dequeue).